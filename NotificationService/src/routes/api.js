'use strict';

// Legacy placeholder now unused; notifications routes are defined in src/routes/notifications.js
const express = require('express');
const router = express.Router();

module.exports = router;
